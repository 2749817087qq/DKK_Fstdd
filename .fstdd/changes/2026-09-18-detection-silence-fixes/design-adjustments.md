# 设计调整记录 — detection-silence-fixes

**change**：2026-09-18-detection-silence-fixes
**Gate**：3（build → deliver）
**日期**：2026-09-21
**维护**：K（经验库归口节点）

> 本文件记录 **build 阶段相对原设计的偏离与调整**，供 Gate 3 确认。
> 原则：**只记录"与 design.md 不一致"或"设计未覆盖但必须做"的项**，逐条给出依据与影响。

---

## A-01 · S4b 的执行主体变更（FSTDD004 → K）

| 项 | 原设计 | 调整后 |
|---|---|---|
| 执行方 | 挂 **FSTDD004**（协作开发） | **K 直接实现** |
| 依据 | 004 于 2026-09-20 17:56 澄清：「本节点每小时轮询自动化**仅负责经验回传闭环**（拉取任务/回传经验/上传回执）；S4b 的 DFX-010/011 fix/gate 代码改动属 **STDD 核心代码改动，须经 D哥 显式派工、走 Gate 流程**，未在轮询中自动执行」 | 004 的澄清成立 ⇒ S4b 属**主仓代码改动**，不在节点自动化职责内 |
| 影响 | 交付延后（原 09-20 13:00 → 实际 09-21 07:2x） | 无质量影响；避免了"等一个不会发生的交付" |

## A-02 · DFX-010 实现口径细化（新增"逐条点名"）

| 项 | 原设计 | 调整后 |
|---|---|---|
| 判据 | 「结束报告含跳过计数 ≥1」 | 保持，并**增加**：逐个点名最多 5 个跳过文件（含原因类型）、`--dry-run` 同样提示跳过数 |
| 依据 | 只报数字仍不足以定位；点名后可直接修复 | 属**加强**，不改变判据语义 |

## A-03 · DFX-011 实现口径细化（单条失败不阻断其余）

| 项 | 原设计 | 调整后 |
|---|---|---|
| 判据 | 「输出含该 spec 警告；其余 spec 正常生成」 | 保持，并**增加**：`_generate_spec` 调用也逐条 `try/except`（原实现中单个 `SystemExit` 会中断整轮循环），并输出「共 N 个未生成，其余 M 个正常」 |
| 依据 | 原代码的 `except SystemExit: pass` 位于**循环外** ⇒ 一条损坏会吞掉整轮（与"其余正常"的判据矛盾） | **这是原设计的隐藏缺口**，本次一并补齐 |

## A-04 · 审计活表（except-points.yaml）同步调整

| 项 | 调整 |
|---|---|
| 移除条目 | **EA-005**（fix.py 静默 skip）、**EA-006**（gate.py 静默 continue）——**二者已由 S4b 发声** |
| meta 更新 | `total: 19 → 17`；`by_classification.意外吞错: 5 → 3` |
| voiced_by_change 追加 | `EA-005 upstream/fstdd/cli/commands/fix.py:64`、`EA-006 upstream/fstdd/cli/commands/gate.py:154` |
| 依据 | 活表与实时扫描的**零漂移校验**（`test_aud_002`）在改后失败 ⇒ 表必须同步；这正是该测试的价值 |
| 纪律沉淀 | 新增 `except` **必须使用异常变量 `e`**，否则仍会被扫描器判为"静默吞错" |

## A-05 · 附带修复：guard 运行时目录豁免（V3.0.6）

| 项 | 内容 |
|---|---|
| 问题 | agent 运行时目录（`.workbuddy-ai/` 等）被流程门禁当作**交付物**拦截，产生**误阻断且零安全价值** |
| 调整 | 增加豁免：`.workbuddy-ai` / `.pytest_cache`；并支持 `.fstdd/config.d/project.yaml` 的 `guard.exempt_paths` 扩展 |
| 是否属本 change 范围 | **严格说属"附带"**（设计未列），但与"消除静默/误判"同一主题，且**只放宽、不收紧**，风险为零 |
| 处置 | 单独提交 `6bd7eed`，便于追溯与独立回滚 |

## A-06 · 版本号提升

| 项 | 值 |
|---|---|
| 由 | `stdd_version: 3.0.5` |
| 到 | **`3.0.6`** |
| 触发 | 本 change 收口（S1/S2S3/S4a/S4b + 附带 guard 豁免） |

---

## 未调整项（与原设计一致）

- S1（guard 六处修复）、S2S3（status/scan 失败有声）、S4a（validate/baseline/batch）——均按原设计交付并已合并。
- 归档表（`2026-09-18-silent-failure-audit`）**不动**（D6 决策），仅维护活表。

## 风险与回滚

| 项 | 说明 |
|---|---|
| 风险 | 低。改动集中在**输出可见性**（新增打印），不改判定逻辑；guard 豁免只放宽 |
| 回滚 | 三个提交独立：`74ad408`(S4b) / `6bd7eed`(guard) / `646fb0c`(change 目录收录)；`git revert` 任一即可 |
| 回归 | 全量 pytest（见 test-report.md 的 Gate 3 记录） |
