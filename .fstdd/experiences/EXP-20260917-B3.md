---
experience_id: EXP-20260917-B3
category: code
severity: high
occurrences: 3
pattern: "naive→aware 改造时写成 _dt.now(_dt.timezone.utc) —— _dt 是 datetime 类、timezone 是模块属性 → 必抛 AttributeError，被外层 except Exception 吞掉 → 检测逻辑静默全哑"
root_cause: |
  `from datetime import datetime as _dt` 之后，`_dt` 是**类**，
  `timezone` 挂在**模块**上而不是类上。`_dt.timezone` 是 AttributeError。
  三处（upgrade.py 僵尸检测无关的 upgraded_at 字段 / guard.py 僵尸 change
  检测 / guard.py 卡壳检测）全部包在 `except Exception: pass`（或 continue）
  里 —— 于是：
  - 僵尸 change 检测永远返回 False（不拦截）
  - 卡壳检测永远不加警告
  - upgraded_at 字段缺失时 upgrade notes 写入静默跳过
  没有任何报错、没有任何日志。「比崩溃更糟」的静默失效教科书案例。
  更糟的是静态检测器（check_timestamps L2）只查「now() 是否带时区参数」
  的形态，`_dt.timezone.utc` 形态上「正确」——检测器给了假安全感。
detection_trigger: "某检测/告警逻辑长期「零命中」；或 naive→aware 改造后相关代码路径从未真跑过"
fix_template: |
  1) 正确写法：`from datetime import datetime as _dt, timezone as _tz`
     → `_dt.now(_tz.utc)`
  2) naive→aware 改造的验收 = **每个改写点真跑一遍所在代码路径**，
     静态正则只查形态、查不到类属性误写这种语义错
  3) 对「长期零命中」的检测逻辑保持怀疑：写一条必触发的变异用例
     （mutation test）验证它真的能响
  4) except 块里至少留 logger.debug 痕迹，别让异常无声消失
tags: [code, datetime, silent-failure, exception-swallowing, mutation-test]
language: python
lifecycle_state: deposited
---

## 现象

全量工程测试首跑：test_constitution_migration 4 个 ERROR
（fixture 里真调 `_write_upgrade_notes`）+ 相关测试红。

## 根因

Slice 3 做时间戳统一时，三处改写写成 `_dt.now(_dt.timezone.utc)`。
单测覆盖的两处没暴露（fixture 路径外的 except 吞掉了），
直到全量套件的真实路径测试才炸出 AttributeError。

## 证据（决定性）

```
AttributeError: type object 'datetime.datetime' has no attribute 'timezone'
  upgrade.py:340  ← _write_upgrade_notes 的 upgraded_at
```
guard.py 两处（196/517）无直接测试，靠修复后人工审查同模式 grep 全仓补抓。

## 一般化

**形态正确的代码语义上可能是错的；语义错被 except 吞掉后等于没写。**
改造类任务的验收标准不是「静态检查过」，而是「每条被改代码路径都有
真实执行记录」。长期零命中的检测逻辑是可疑资产，要用变异测试证伪
「它真的在干活」。
