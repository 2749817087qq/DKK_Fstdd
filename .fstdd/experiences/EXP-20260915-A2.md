---
experience_id: EXP-20260915-A2
category: tooling
severity: medium
occurrences: 2
pattern: Windows 上 pkill 无法终止 ssh.exe，留下幽灵进程占住转发端口，下次建隧道报 Address already in use
root_cause: |
  Git Bash 的 pkill 依赖 POSIX 进程模型，匹配不到 Windows 原生进程（ssh.exe）。
  命令返回成功但进程实际仍在，端口持续被占用。
detection_trigger: "建立 SSH 动态转发时报 bind: Address already in use，且 pkill 后依然如此"
fix_template: |
  改用 OpenSSH 自带的 control socket 管理连接：
    建立：ssh -f -N -M -S <sock路径> -D <port> ...
    关闭：ssh -S <sock路径> -O exit <user@host>
  比 pkill 可靠且跨平台一致。另可加 ExitOnForwardFailure=yes 让端口冲突立即报错而非静默失败。
tags: [ssh, windows, tunnel, process-management]
language: bash
---

## 现象

在 Windows 的 Git Bash 中：

```bash
pkill -f "ssh.*-D 18080"     # 报成功
netstat -ano | grep 18080    # 端口仍被占用
```

下次建隧道：`bind [127.0.0.1]:18080: Address already in use`

## 根因

Git Bash 的 `pkill` 面向 POSIX 进程模型，无法匹配 Windows 原生进程（`ssh.exe`）。
它返回成功只是"没报错"，不代表杀掉了。

## 处理

用 OpenSSH 的 control socket（连接复用机制）：

```bash
# 建立（-M 开启 master，-S 指定 socket）
ssh -f -N -M -S /tmp/t.sock -D 18080 -o ExitOnForwardFailure=yes user@host

# 关闭（-O exit 通过 socket 通知主连接退出）
ssh -S /tmp/t.sock -O exit user@host
```

实测：建立后 `ssh -O exit` 能干净关闭，端口立即释放，无残留进程。

## 附带建议

- `ExitOnForwardFailure=yes`：端口冲突时立即失败，避免"以为建好了其实没建"
- 脚本里可加"自动找空闲端口"逻辑，作为端口被占的兜底
