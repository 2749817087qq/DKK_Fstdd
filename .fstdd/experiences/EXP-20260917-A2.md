---
experience_id: EXP-20260917-A2
category: test
severity: medium
occurrences: 1
pattern: "用「文本里是否出现某字符串」判定「行为是否违规」，而检测代码本身必须提及该字符串 —— 断言命中了检测规则自己"
root_cause: |
  把「文本出现」当成了「行为发生」。但两者并不等价：
  · 注释里正当地说明「为什么不用 --force」会提及 `--force`
  · 检测违规的代码（如 grep 模式）**必须**写出该模式字符串
  于是断言被自己的检测规则满足，形同虚设 —— 且**测试仍然全绿**，不会有人发现。
detection_trigger: "断言 `\"X\" not in 源码` 类否定式检查；或断言用关键词匹配而非锚定具体措辞"
fix_template: |
  1) 断言针对**行为载体**而非**文本出现**：判定「有无强制推送」应扫真正的
     `git ... push` 命令行（用正则 `\\bgit\\b.*\\bpush\\b` 先筛选出行），
     而不是全文找 `--force`
  2) 必须扫全文时，**先剔除注释行**（本项目已有 `_code_only()` 辅助函数）
  3) 对任何「否定式断言」，**做一次变异测试**：注入一个真实违规，确认断言会红。
     绿着通过不算数 —— 要证明它**能**变红
  4) 同类陷阱：断言用**短关键词**（如 `"timeout"`）时，可能被
     pytest 的 tmp_path 目录名（`test_xxx0`）意外命中 → 改为锚定被测代码自身的措辞
tags: [testing, self-match, assertion-validity, mutation-testing]
language: python
---

## 现象

Slice H 的守护测试断言「端到端脚本不得含强制推送」：

```python
assert "--force" not in code, "含强制推送"
```

**测试失败**，但失败原因是它自己：

```
E   AssertionError: 含强制推送
E     '--force' is contained here:
E       E 'push .*--force|push .*-f[[:space:]]|push .*--mirror'
```

被命中的那行是端到端脚本里**用来审查服务器钩子是否含强制推送**的 grep 模式 ——
它是**检测**强制推送的正当代码，不是执行它。断言把「检测规则」当成了「违规行为」。

## 同类现象（本 change 内两例）

| 位置 | 断言 | 被什么意外满足 |
|---|---|---|
| Slice H 守护测试 | `"--force" not in code` | 脚本内**检测** `--force` 的 grep 模式自身 |
| Slice A 超时测试（更早） | `"timeout" in r.stdout.lower()` | pytest tmp_path 目录名 `test_hook_timeout_counts_as_failure0` |

第二例更隐蔽：钩子会打印故障标记的**文件路径**，而该路径含 tmp_path 目录名，
目录名又恰好含测试函数名 —— 于是**变异注入后断言仍然通过**，靠变异测试才揪出来。

## 一般化

**「测试绿了」不等于「断言有效」。** 对每个否定式断言，问一句：

> 我注入一个真实违规，它会红吗？

答不上来就去做变异测试。本项目已用此法揪出 3 处形同虚设的断言
（含一处 `or` 短路掩盖、一处 tmp_path 命中、一处自匹配）。
