---
experience_id: EXP-20260915-B1
category: tooling
severity: high
occurrences: 1
pattern: "改包名时只替换静态 import 语句，漏掉以字符串形式存在的动态导入路径，导致运行时 ModuleNotFoundError"
root_cause: |
  Python 的动态导入（importlib.import_module("pkg.cli.commands.xxx")）把模块路径
  写成**字符串字面量**，通常集中在一张命令映射表里。基于 AST 或 `^import`/`^from`
  的正则替换都匹配不到这类引用，于是静态检查全绿、一运行就崩。
detection_trigger: "包目录改名后，CLI 报 ModuleNotFoundError: No module named '<旧包名>'"
fix_template: |
  改包名时同时处理三类引用：
    1) 静态 import  —— `^\\s*(import|from) <旧包>\\.`
    2) 动态导入字符串 —— `[\"']<旧包>\\.`（importlib / __import__ / entry_points）
    3) 配置与文档中的路径字符串
  第 2 类最容易漏，建议用 `grep -rn "[\"']<旧包>\\."` 单独扫一遍。
tags: [refactor, python, dynamic-import, rename, packaging]
language: python
---

## 现象

把 Python 包 `stdd` 改名为 `fstdd` 后：

- 39 个文件的静态 import 已全部替换
- `grep -rnE "^\s*(import|from)\s+stdd\b"` 返回**空**
- 但 `fstdd --help` 正常、`fstdd init` 崩溃：

```
File ".../fstdd/cli/__init__.py", line 483, in main
    mod = importlib.import_module(mod_path)
ModuleNotFoundError: No module named 'stdd'
```

## 根因

`cli/__init__.py` 里有一张命令映射表：

```python
commands = {
    "knowledge": "stdd.cli.commands.knowledge.cmd_knowledge",
    "bootcamp":  "stdd.cli.commands.bootcamp.cmd_bootcamp",
    ...
}
mod_path, func_name = commands[args.command].rsplit(".", 1)
mod = importlib.import_module(mod_path)
```

模块路径是**字符串**，不是 import 语句 —— 静态替换的盲区。

## 处理

补一条针对字符串字面量的替换：

```python
PAT = re.compile(r'(["\'])stdd\.cli\.')
new, n = PAT.subn(r'\1fstdd.cli.', text)   # 实测命中 34 处
```

## 一般化

**为什么 `--help` 通过而 `init` 失败**：`--help` 只走 argparse，不触发子命令的动态加载；
只有真正执行子命令才会 `import_module`。所以「顶层命令可用」不能证明「包改名成功」，
**必须跑至少一个真实子命令**。

这条也是「静态检查全绿 ≠ 运行时可用」的典型例子。
