# 镜像失败告警 v1.0 任务清单

> 与 `slices.md` 一一对应。优先级：P0 阻塞性 / P1 本阶段应完成 / P2 可延后。

## A. 钩子核心：回显 + 结构化告警块（P0）✅ 完成（7/7 TC）

- [x] A.1 把 HOOKBODY 的输出重定向由 `{ ... } >> "$LOG" 2>&1` 改为 `{ ... } 2>&1 | tee -a "$LOG"`
      —— 既落盘又回显给推送方（REQ-001 / SC-001、SC-002）
- [x] A.2 实测确认 `tee` + `set -o pipefail` 组合下钩子退出码仍为 0，且推送方确实能看到输出
      —— 由 `test_mirror_alert_ops.py` **真实执行钩子**断言 stdout，非文本匹配
- [x] A.3 成功与失败使用**不同**标识：成功 `[MIRROR-OK]`、失败 `[MIRROR-FAILED]`，不共用语义
- [x] A.4 失败时输出结构化告警块：明写「裸库已更新 / GitHub 未同步 / 对外通道滞后」
      + 失败项 + 失败原因（SC-003）
- [x] A.5 分支推送与 tag 推送**分别**判定，部分失败时明确指出是哪一项失败（SC-004）
- [x] A.6 超时路径（`timeout 180/120` 触发 rc=124）与失败路径同等处理，不得静默通过
- [x] A.7 新增 `upstream/tests/test_mirror_alert_ops.py`（7 个 TC）：从部署脚本提取真实 HOOKBODY，
      用 stub `git`/`timeout` **真实执行钩子**，断言推送方视角的 stdout 与落盘日志
- [x] A.8（偏离 ADJ-001）钩子路径改为可经 `FSTDD_BARE_DIR` / `FSTDD_MIRROR_LOG` / `FSTDD_MIRROR_FLAG`
      环境变量覆盖（带默认值）—— 否则钩子在非服务器环境无法被自动化验证；生产行为不变

## B. 故障标记（P0）✅ 完成（3/3 TC）

- [x] B.1 失败时写 `mirror-failed.flag`，内容为**键值行**：
      `failed_at=` / `failed_items=` / `failed_detail=` / `bare_head=`（SC-005、SC-007）
- [x] B.2 全部成功时 `rm -f` 该标记，无需人工介入（SC-006）
- [x] B.3 测试：TC-MFA-008（写入 + 含时间戳与失败项）、TC-MFA-009（恢复后**自动清除**）、
      TC-MFA-010（键值行可结构化解析，不依赖自然语言正则）

## C. 既有语义守护（P0）✅ 完成（2/2 TC）

- [x] C.1 断言钩子恒 `exit 0`：TC-MFA-019 用 **5 组**失败组合（含 124/128）真实执行钩子，
      逐组断言退出码为 0（SC-013）
- [x] C.2 断言钩子推送不含 `--force` / `--force-with-lease` / `-f` / `+refspec`（SC-014）
      —— **剔除注释后**审查，避免注释里正当提到 `--force` 造成误报
- [x] C.3 真实推送的返回码断言由 Slice H 在服务器上完成（本切片以真实执行钩子覆盖）

## D. 巡检命令（P0）✅ 完成（6/6 TC）

- [x] D.1 新增 `tools/check_mirror.sh`：**单次 ssh 往返**采集裸库 sha / GitHub sha / flag 状态（SC-008、SC-009）
- [x] D.2 三态判定与退出码：**收敛=0 / 滞后=2 / 无法测量=3**（SC-008）
- [x] D.3 输出同时含三项事实 + 明确的「判定」行，无需人工解读（SC-009）
- [x] D.4 只读幂等：连续两次执行输出一致；脚本内无任何写操作（TC-MFA-014）
- [x] D.5 `git ls-files -s` 为 `100755`；依赖限于 `bash`/`git`/`ssh` 与 coreutils，**不含 `jq`**（SC-018 / TC-MFA-024）
- [x] D.6 前置条件缺失（ssh 别名未配）时给出明确提示，退出码 3 与「滞后」2 可区分（TC-MFA-025）

## E. 控制面接入（P0）

- [ ] E.1 部署脚本新增**幂等**的基础设施节点注册（`POST /nodes/register`），与 agent 节点身份区分（SC-010）
- [ ] E.2 HOOKBODY 失败时 `POST /messages`（`kind=notice`）投递告警，body 含失败项与发生时间（SC-011）
- [ ] E.3 上报带短超时 + `|| true` 兜底：控制面不可达时回显与标记仍生效、退出码仍为 0、推送不被明显延迟（SC-012）
- [ ] E.4 测试：节点注册幂等（同 id 注册两次不产生重复记录）；上报失败不影响钩子退出码

## F. 文档同步（P1）

- [ ] F.1 `docs/DISTRIBUTED_ACCESS.md` 日常操作表新增「查镜像状态」的一条命令
- [ ] F.2 故障处置表补充镜像失败的完整处置路径（flag + 巡检命令 + 恢复方式）
- [ ] F.3 删除「必须人工 tail 日志才知道镜像失败」的表述（原第 84 / 113 行须已改写）
- [ ] F.4 测试：文档含该命令、含处置路径、不含被禁表述

## G. 不破坏既有约束（P0）

- [ ] G.1 全量 pytest 通过，与基线 592 passed 一致，无新增失败/跳过
- [ ] G.2 仓库凭证扫描命中数为 0（`ghp_` / `github_pat_` / `sk-` / `AKIA` / 私钥头）
- [ ] G.3 `verify_eol.py` / `verify_skill_standards.py` 通过；本 change 目录全 LF

## H. 端到端故障链路（P0 · 服务器实测）

- [ ] H.1 部署新钩子与巡检命令到服务器（幂等，覆盖式）
- [ ] H.2 注册基础设施节点身份并确认 `GET /nodes` 可检索
- [ ] H.3 正常推送一次：确认推送方输出出现镜像结果、`mirror.log` 同步落盘
- [ ] H.4 **故意制造失败**（临时改 `github` remote 指向不可达地址），推送一次
- [ ] H.5 验证四层出口：① 推送方输出有告警块 ② `mirror.log` 有记录 ③ flag 已写 ④ `GET /messages` 有 notice
- [ ] H.6 巡检命令在故障态给出「滞后」判定与正确退出码
- [ ] H.7 **恢复现场**：还原 remote → 再推一次 → 确认 flag 被自动清除、输出恢复成功标识
- [ ] H.8 巡检命令回到「已收敛」判定与退出码 0
- [ ] H.9 新增 E2E 脚本 `tools/e2e_mirror_alert.sh`（幂等 + 含强制恢复步骤），并配 1 个守护测试

## I. 测试与验证

- [ ] I.1 每个切片的 TC 覆盖率 100%（对照 `test-plan.md` 的 25 个 TC）
- [ ] I.2 每个切片新增测试数 > 0，且全部通过
- [ ] I.3 全量 pytest 无回归
- [ ] I.4 `test-report.md` 生成（TC 覆盖率 planned vs actual、逐切片状态、失败模式检查、已知问题）
