# 法定源收进工作区 测试报告

> 变更：`2026-09-17-canonical-in-workspace`
> 执行日期：2026-09-17
> 执行环境：Windows，Python 3.11.9（`C:\Python311\python.exe`）
> 测试工具：`pytest` + 三个 verify 脚本 + git 状态与哈希比对

## 一、执行结果总览

| 指标 | 结果 |
|---|---|
| 全量用例总数 | **543** |
| 通过 | **543** |
| 失败 | 0 |
| 退出码 | 0 |
| 基线（本变更开始时） | 526 passed |
| 本变更新增 | **+17**（`upstream/tests/test_canonical_in_workspace.py`） |

> 526 = 上一轮 `install-source-repo` 的 503 + `inbox-review-sync` 新增的 23。
> 543 = 526 + 本变更的 17。

### 1.1 三项校验

| 校验 | 结果 |
|---|---|
| `verify_eol.py` | **7/7** |
| `verify_skill_standards.py` | **7/7** |
| `verify_rename.py` | **8/8** |

### 1.2 位置与一致性

| 项 | 结果 |
|---|---|
| 法定源 | 工作区内 `stdd-repo`（HEAD `00ef79f`，完整历史 + tag `fstdd-v1.0.0`） |
| GitHub | `00ef79f`，tag `fstdd-v1.0.0` |
| 已安装 skill 含 `stdd-repo` | 6/7（第 7 个 `fstdd-fin` 是领域层，不引用 upstream 路径） |
| 已安装 skill 含 `~/.workbuddy-ai/Fstdd` | **0** ✓ |

## 二、逐项验证（对应 test-plan 的 14 个 TC）

### 功能 1：收拢不丢内容

| TC | 验证内容 | 结果 |
|---|---|---|
| TC-CIW-001 | 区外副本无独有内容 | ✅ 见下方比对表 |
| TC-CIW-002 | 法定源是工作区仓库且历史完整 | ✅ 58+ 提交 + tag |

**TC-CIW-001 比对结果**（按 git blob 哈希判定「内容是否已在仓库对象库中」）：

| 分类 | 数量 |
|---|---|
| 与 `stdd-repo` 逐字节相同 | 727 |
| 内容在 `stdd-repo` 对象库中 | 2 |
| 「独有」 | 4 |

那 4 份「独有」全部是 `experiences/*.md` —— **生成物**（`stdd-repo` 侧被 gitignore）。
它们**已随本次归档一并保留**，未丢失。

> 附带发现：这 4 份与上一轮备份中的同名文件**内容不同**，说明它们在上轮之后被重新生成过。
> 本次归档已捕获最新版本。

### 功能 2：校验解析顺序

| TC | 验证内容 | 结果 |
|---|---|---|
| TC-CIW-003 | 自定位优先 | ✅ 源码顺序正确 |
| TC-CIW-004 | `verify_rename` 同顺序 | ✅ |
| TC-CIW-005 | 解析结果落到工作区 | ✅ `INSTALLED_TOOLS = <workspace>/stdd-repo/tools` |
| TC-CIW-006 | 三项校验全绿 | ✅ 7/7、7/7、8/8 |
| TC-CIW-007 | 不依赖区外副本 | ✅ 解析结果为工作区仓库，**完全不涉及区外副本** |

### 功能 3：归档完整性

| TC | 验证内容 | 结果 |
|---|---|---|
| TC-CIW-008 | 归档前后一致 | ✅ **829 = 829 文件，逐文件 md5 完全一致** |
| TC-CIW-009 | 原目录未被删除 | ✅ 「归档不删」 |

**归档位置**：`backups/canonical-archived-20260917-083330/Fstdd`

### 功能 4：skill 路径与安装源

| TC | 验证内容 | 结果 |
|---|---|---|
| TC-CIW-010 | skill 路径指向工作区 | ✅ 0 个指向区外副本；`verify_workbuddy_skills` PASS |
| TC-CIW-011 | 安装 SRC 解析 | ✅ 解析为 `stdd-repo/upstream`（未设 `FSTDD_SRC`） |

### 功能 5：文档与记忆

| TC | 验证内容 | 结果 |
|---|---|---|
| TC-CIW-012 | 文档第 0 节反映新架构 | ✅ 法定源写为工作区 `stdd-repo`；说明区外副本已归档；保留「GitHub 是上传目标」 |
| TC-CIW-013 | 项目记忆已更新 | ✅ 含「解析顺序随安装源走」的对照表 |

### 功能 6：断言有效性

| TC | 验证内容 | 结果 |
|---|---|---|
| TC-CIW-014 | 注入缺陷必须被捕获 | ✅ **3/3 全部捕获**（见第三节） |

## 三、变异验证

| 注入缺陷 | 结果 | 捕获者 |
|---|---|---|
| 解析顺序改回「区外副本优先」 | ✅ 捕获（2 用例红） | `test_a1`、`test_a5` |
| rename 检查不再排除 `inbox` | ✅ 捕获 | `test_e1` |
| 文档把法定源改回区外副本 | ✅ 捕获 | `test_b1` |

**捕获率 3/3**；变异已全部还原（无 `MUTANT` 残留、无 `.mutbak` 残留）。

## 四、顺带修复的 3 个既有缺陷（详见 design-adjustments D1）

| 缺陷 | 严重度 | 说明 |
|---|---|---|
| `test_fstdd_matrix.py` **硬编码日期** | 🔴 高 | 6 处硬编码 `2026-09-16-demo-change` → 该文件**只在 2026-09-16 当天能过**，次日必然失败（本次实测 3 failed）。已改为动态日期 |
| `verify_rename` 把运行产物当源码扫 | 🟡 中 | `inbox/`（gitignored 运行产物）内含历史经验文档，合法提到旧名 → 误报「残留 20 处」。`EXCLUDE_DIRS` 补 `inbox` |
| 上一轮 change 的 3 处断言编码旧决策 | 🟡 中 | 随架构反转同步更新，并在 docstring 写明理由 |

## 五、失败模式检查

| 风险区域 | 结论 |
|---|---|
| **归档时丢内容**（🔴 唯一不可逆点） | ✅ 未发生。归档前已按 git blob 哈希证明无独有内容；归档后 829=829 逐文件哈希一致 |
| 解析顺序与重装次序反了 | ✅ 未发生。按 Decision 4 先改顺序再重装；三项校验全绿 |
| **误改 D 盘调试副本** | ✅ 全程只读（仅存在性检查） |
| 归档操作不可逆 | ✅ 用复制而非移动；原目录保留 |
| 全量测试回归 | ✅ 543 passed（中途 6 failed，已定位并修复，见第四节） |

## 六、遗留与已知限制

| # | 项 | 说明 |
|---|---|---|
| L1 | **未删除**区外副本 | 按 D哥 决定「归档不删」。处置由 D哥 决定 |
| L2 | 区外副本仍被…… | 已确认**无** skill 引用它（0 个）。仅作历史归档 |
| L3 | `D:/Programs/DKK_Fstdd` | 另一程序的调试副本，**全程未动** |
| L4 | 未在另一台机器验证 | 「工作区仓库 → 安装 → 校验」全链路只在本机验证过 |
| L5 | 已知代价 | skill 内固化路径指向**工作区目录**（会话目录）—— 这是「真源放工作区」的必然结果。若该目录变动，需重装（`FSTDD_SRC`/`FSTDD_INST_DIR` 均可显式覆盖） |

> **L5 是本决定的核心权衡**：上一轮为避免它而把真源放到区外；D哥 权衡后
> 选择「真源必须在工作区内」（防 C 盘重装丢资料，见后续 change）。
> 两者不可兼得，本变更按 D哥 的决定执行。

## 七、结论

- 全量 **543 passed / 0 failed**。
- 三项校验全绿：7/7、7/7、8/8。
- **归档完整性**：829 = 829 文件，逐文件 md5 一致；原目录保留。
- **变异验证 3/3 全部捕获**，断言具备真实区分度。
- 顺带修复 3 个既有缺陷（含一个「只在当天能过」的严重测试缺陷）。
- 本地与 GitHub 一致（`00ef79f` + tag）。

**本变更可交付。**
