# 本机私有配置：由 stdd init 生成，含本机路径，不入库
.claude/

# WorkBuddy 的项目数据目录（记忆、临时文件），属本机运行态，不入库
# 防御性规则：正常位置在工作区根，不在本仓库内；万一被创建在这里也不该提交
.workbuddy-ai/

# 经验导出产物：由 tools/share_experience.py 生成，属运行产物不入库
# （源是 .fstdd/experiences/，回传目标是独立仓库 Fstdd-experiences）
#
# ⚠️ 2026-09-24 修正：原写法 `experiences/` 是**非锚定模式**，会把任意层级的 experiences/
# 一并忽略 —— 包括作为「源」的 .fstdd/experiences/（实测 61 份只跟踪到 12 份），
# 导致知识图谱 merge 的输入无法入库。改为锚定写法：只忽略根级产物 + 上游备份快照内的同名目录。
/experiences/
upstream/.fstdd/backup/*/.stdd/experiences/

# 经验回传审计记录：append-only，属本机运行态（含本机路径），不入库
.fstdd/share-audit.yaml

# 待审核池同步的暂存区：由 tools/inbox_pull.py 生成，属运行产物不入库
# （raw=端点取回；staging=归一+剥离+脱敏后待发布；rejected=被拒留痕）
inbox/

# Python 运行缓存
__pycache__/
*.py[cod]
