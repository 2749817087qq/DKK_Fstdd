---
category: process_design
confidence: 0.85
detection_trigger: thorough/standard 模式的 review_agents > 0，但没有任何 agent 输出记录
experience_id: EXP-2026-0012
first_seen: '2026-07-06'
fix_template: |
  1. 长程模式下 Verify 阶段的 review agent 仍需运行（并行不阻塞主流程）
  2. Gate 3 前汇总所有 agent 发现（即使全部 PASS 也要展示）
  3. 在 .fstdd.yaml 中记录 review agent 的实际运行状态和发现数
  4. 如果 review agent 发现 HIGH 问题，自动降级为交互模式要求用户确认
language: python
last_seen: '2026-07-06'
lifecycle_state: discovered
occurrences: 2
pattern: 长程模式跳过 Verify 阶段的深度审查（review agent/pass@k/安全扫描）
root_cause: |
  长程模式的设计初衷是将 Phase 3-5 的"交互确认"合并为一次性预授权，
  提升执行效率。但实践中"预授权"被错误扩展为"跳过审查"——
  thorough 模式要求的 3 review agent + pass@k=3 在长程模式下全部未执行。
  
  根因在于长程模式的权限模型只覆盖了"用户交互"（AskUserQuestion/Gate），
  没有区分"自动化质量检查"（review agent/pass@k/安全扫描）。
  后者不需要用户交互，完全可以在后台并行运行。
severity: high
source_change: "2026-07-06-phase0-scaffold"
source_file: ''
tags:
  - long-range
  - verify
  - process
  - review-agent
---
