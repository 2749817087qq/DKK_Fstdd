---
category: verify_degradation
confidence: 0.9
detection_trigger: verify 阶段只有 import 语句，没有 assert 或 pytest 运行
experience_id: EXP-2026-0011
first_seen: '2026-07-06'
fix_template: |
  1. 每个模块至少 1 个"给定输入→验证输出"的单元测试
  2. Verify 阶段必须运行 pytest（不只是 python -c "import xxx"）
  3. CLI 模块必须用 CliRunner 测试 --help 和关键子命令
  4. Skill 文件必须实际调用一次验证参数有效性
language: python
last_seen: '2026-07-06'
lifecycle_state: discovered
occurrences: 6
pattern: 导入检查冒充功能验证（import ≠ test）
root_cause: |
  开发者在快速迭代中将"模块可导入"等同于"功能正确"。
  Phase 0-6 的每个 Verify 步骤都只执行了 `python -c "from daostock.xxx import yyy; print('[OK]')"`，
  这无法检测：函数逻辑错误（BaoStock date replace 无操作）、CLI 参数不匹配（--days vs --start/--end）、
  数学异常（零标准差除零）。
  长程模式的"全自动执行"进一步鼓励了这种最简验证——因为开发者知道中间不会有用户打断。
severity: critical
source_change: "2026-07-06-phase6-hk-polish"
source_file: ''
tags:
  - verify
  - testing
  - quality
  - process
  - long-range
---
