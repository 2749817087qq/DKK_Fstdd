---
category: contract_gap
confidence: 0.9
detection_trigger: Skill 文件中引用的 CLI 参数在实际 Click 命令定义中不存在
experience_id: EXP-2026-0013
first_seen: '2026-07-06'
fix_template: |
  1. 在 `daostock install` 时自动解析每个 Skill 中的 Bash 命令块
  2. 提取命令和参数（如 `daostock data history --days 365`）
  3. 用 CliRunner 执行 `command --help` 验证参数有效性
  4. 参数不匹配时：WARNING 并列出正确参数
  5. 将 Skill-CLI 契约检查作为 Phase 3 Skills 的 Definition of Done
language: python
last_seen: '2026-07-06'
lifecycle_state: discovered
occurrences: 3
pattern: Skill 中的 Bash 命令与 CLI 实际参数签名不一致（Skill-CLI 契约断裂）
root_cause: |
  Skill 文件（SKILL.md）和 CLI 命令（Click 装饰器）是两个独立的代码路径，
  没有编译时或运行时的契约检查。Phase 3 编写 Skill，Phase 4 修改 CLI 时，
  没有人重新验证 Phase 3 的 Skill 是否仍然有效。
  
  具体案例：
  - Skill 引用 `data history --days 365`，但 CLI 定义为 `--start/--end`
  - Skill 引用 `python -c "import yaml; ..."`，其中 `...` 是字面占位符
  - Skill 引用 `portfolio optimize`，但 CLI 未实现该子命令
severity: high
source_change: "2026-07-06-phase3-skills"
source_file: ''
tags:
  - skill
  - cli
  - contract
  - integration
  - regression
---
