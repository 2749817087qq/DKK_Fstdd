---
experience_id: EXP-20260915-A6
category: process
severity: high
occurrences: 1
pattern: 拿到一份「问题清单」就照单执行，结果去修了并不存在的问题
root_cause: |
  报告/审计清单往往基于**某一时点**的状态。若期间代码有演进（新增目录、修改文件），
  清单中的部分条目会失效。此外，清单作者可能用了过于粗糙的检测手段
  （如用 grep -c "^---$" 判断 frontmatter 重复，把正文水平线也数进去）。
detection_trigger: 清单条目与当前代码状态对不上；或按清单"修复"后发现无需改动
fix_template: |
  对每一条独立实测核实，再决定是否动手：
    1) 用可复现的命令验证问题是否真实存在（不要凭清单描述）
    2) 用能区分「现象」与「实质」的检测手段（如解析而非计数）
    3) 核实结论分三类：成立需修 / 成立但不该我们修 / 不成立
  实测后再动手 —— 去"修"不存在的问题，本身就是引入风险。
tags: [process, audit, verification, code-review]
language: python
---

## 现象

收到一份对照规范的 7 项问题清单。照单执行前逐项实测，结果：

| 清单条目 | 实测结论 |
|---|---|
| 目录缺失 | 不成立（目录存在，引用链完整） |
| frontmatter 重复 | 不成立（只有 1 个块，其余是正文水平线） |
| 版本不一致 | 成立，但属第三方 skill 且是双市场记录并存，不该改 |
| 数据外发 | 已解决（策略已在位） |
| 字段缺失 | 成立，已修 |
| 路径过时 | 成立且比描述更严重（硬编码本机绝对路径），已修 |

**7 项中只有 2 项需要动手。**

## 两个典型误判

**1. 计数当解析**

判断 frontmatter 是否重复时用 `grep -c "^---$"`，得到 7~11 个，
看似"严重重复"。但 `---` 在 Markdown 里也是水平分隔线。
正确做法是**解析**：从文件开头逐个匹配 frontmatter 块，看是否连续出现。

> 计数是过程指标，不是正确性指标。

**2. 忽略状态演进**

"目录缺失"的结论可能来自清单生成时点——之后目录被建好了。
清单不会自动更新，**必须重新核实**。

## 一般化

**清单是线索，不是结论。** 对待任何外部审计结果（包括 AI 生成的），
默认动作应该是"核实"，而不是"执行"。

反过来也成立：自己产出的清单要注明**生成时点与依据**，
否则下游会拿着过期结论去改代码。
