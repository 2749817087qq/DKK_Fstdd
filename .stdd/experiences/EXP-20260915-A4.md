---
experience_id: EXP-20260915-A4
category: quality
severity: high
occurrences: 1
pattern: 脱敏正则把文件名当域名替换，导致导出文档内容失去意义
root_cause: |
  用 `\b(?:[a-z0-9-]+\.)+[a-z]{2,}\b` 匹配域名时，README.md、design.yaml、config.json
  这类「单词.后缀」形式同样命中，被替换成 <DOMAIN>。
detection_trigger: 导出后发现正文里出现 <DOMAIN>，而原文该处本应是文件名
fix_template: |
  域名判定前先排除已知文件扩展名（FILE_EXTS 集合），再判断是否在公共域名白名单内，
  两者都不满足才替换。同时必须用真实数据跑一遍检查误伤，不能只看「命中了几条」。
tags: [sanitize, regex, data-export, privacy]
language: python
---

## 现象

导出经验/文档并脱敏后，正文变成：

```
**现象**：GREEN 首轮报 `索引 diff 1 个文件：<DOMAIN>`。     ← 原文是 README.md
**处理**：引入 `ALLOWED_DIFF = {"<DOMAIN>"}`               ← 原文是 {"README.md"}
```

内容完全失去意义。

## 根因

域名正则 `(?:[a-z0-9-]+\.)+[a-z]{2,}` 对 `README.md` 是完美匹配：
`README` 匹配 `[a-z0-9-]+`，`.md` 匹配 `\.[a-z]{2,}`。

**问题不在于正则写错，而在于把"形似"当成了"实质"。**

## 处理

```python
FILE_EXTS = {"md","py","txt","json","yaml","yml","sh","ps1","log","lock", ...}

def _dom(m):
    raw = m.group(0)
    if raw.rsplit(".", 1)[-1].lower() in FILE_EXTS:
        return raw          # 文件名，不动
    if raw.lower() in PUBLIC_DOMAINS:
        return raw          # 公共域名，不动
    return "<DOMAIN>"
```

## 关键教训

**脱敏规则必须拿真实数据跑一遍，看误伤，不能只看"命中了几条"。**

第一轮我看到输出「脱敏命中: 域名」时以为是对的——实际上那些"命中"全是误伤
（把 README.md 当域名了），真正的敏感信息（绝对路径、IP）反倒因为样本里没出现而没被检验到。

「命中数」是过程指标，不是正确性指标。
